services:
  nginx:
    build: .
    container_name: blog-nginx
    ports:
      - "80:80"
      - "443:443"
    volumes:
      - /home/ubuntu/blog-project/nginx/ssl:/etc/nginx/ssl:ro
    restart: unless-stopped
